// GameModal is no longer used — games open directly in new tabs via window.open()
// Kept as empty export to avoid import errors during transition
export default function GameModal() { return null; }
