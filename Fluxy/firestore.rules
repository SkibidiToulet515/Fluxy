rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // The app uses server-side Firebase Admin SDK only.
      allow read, write: if false;
    }
  }
}
